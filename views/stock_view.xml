<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_inventory_form_pos_gt" model="ir.ui.view">
            <field name="name">stock.inventory.form.pos_gt</field>
            <field name="model">stock.inventory</field>
            <field name="inherit_id" ref="stock.view_inventory_form"/>
            <field name="arch" type="xml">
                <data>
                    <button name="action_validate" position="replace">
                        <button name="action_validate" states="confirm" string="Validate Inventory" type="object" class="oe_highlight" groups="stock.group_stock_manager,point_of_sale.group_pos_user"/>
                    </button>
                </data>
            </field>
        </record>

        <menuitem
            parent="point_of_sale.menu_point_of_sale"
            action="stock.action_inventory_form"
            id="menu_guateburger_ajuste"
            sequence="40"
            string="Ajustes de inventario"
            groups="point_of_sale.group_pos_user" />


    </data>
</odoo>
